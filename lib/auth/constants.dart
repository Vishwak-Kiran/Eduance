import 'package:flutter/material.dart';

const Color login_bg = Color(0xFF336699);
const Color signup_bg = Color(0xFFC0904D);

const double defaultPadding = 16.0;
const Duration defaultDuration = Duration(milliseconds: 300);
